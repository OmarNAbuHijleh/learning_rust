use fitness::GymWorkout; // NOTE: When reaching into the "lib.rs" file, we have to use the name of the project from the toml as our reference point to access the lib.rs contents!
fn main() {
    let gym_workout = GymWorkout::new();
}
